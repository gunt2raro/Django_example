{% extends 'base.html' %}
{% load crispy_forms_tags %}



{% block content %}

	<h1>{{ title }}</h1>

	
	{% for m in messages %}
	 	<p>{{ m }}</p>
	{% endfor %}

	{% if not form == None %}
		<div class="row">
			<div class="col-md-4">
				<form method='POST' aciton=''>{% csrf_token %}
					{{ form|crispy }}
					<input type="submit" value="Sign Up">
				</form>
			</div>
	{% endif %}

{% endblock %}
